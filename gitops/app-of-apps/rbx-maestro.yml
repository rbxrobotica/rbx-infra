# ArgoCD Application: rbx-maestro
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rbx-maestro
  namespace: argocd
  labels:
    app.kubernetes.io/name: rbx-maestro
    app.kubernetes.io/component: application
    app.kubernetes.io/part-of: rbx-maestro
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: rbx-applications

  source:
    repoURL: https://github.com/rbxrobotica/rbx-infra
    targetRevision: main
    path: apps/prod/rbx-maestro

  destination:
    server: https://kubernetes.default.svc
    namespace: rbx-maestro

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
