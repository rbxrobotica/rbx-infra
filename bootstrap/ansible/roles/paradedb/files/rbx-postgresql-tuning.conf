# Managed by Ansible (rbx-infra: roles/paradedb). Do not edit on the host.
# Loaded via postgresql.conf include_dir = 'conf.d'.
#
# max_connections raised from the stock 100: the shared fleet (robson,
# ZITADEL, PowerDNS, commerce, comms, ...) was measured at 89-101 concurrent
# connections in 2026-07/08, causing reconnect failures at the margin.
# Requires a PostgreSQL restart to take effect. Host has 23 GB RAM.
max_connections = 200
