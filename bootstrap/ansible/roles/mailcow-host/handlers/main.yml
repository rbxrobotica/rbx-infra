---
- name: mailcow up
  command: docker compose up -d
  args:
    chdir: "{{ mailcow_install_dir }}"

- name: restart postfix-mailcow
  command: docker compose restart postfix-mailcow
  args:
    chdir: "{{ mailcow_install_dir }}"
