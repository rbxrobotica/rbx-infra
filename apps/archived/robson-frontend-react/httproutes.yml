# HTTPRoute: Robson Frontend
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: robson-frontend-route
  namespace: robson
  labels:
    app.kubernetes.io/name: robson-frontend-route
    app.kubernetes.io/part-of: robson
spec:
  parentRefs:
    - name: robson-gateway
      namespace: robson
  hostnames:
    - app.robson.rbx.ia.br
  rules:
    - backendRefs:
        - name: robson-frontend
          port: 80
---
# HTTPRoute: Robson API (MIG-v3#2 — points to robsond, not Django)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: robson-api-route
  namespace: robson
  labels:
    app.kubernetes.io/name: robson-api-route
    app.kubernetes.io/part-of: robson
spec:
  parentRefs:
    - name: robson-gateway
      namespace: robson
  hostnames:
    - api.robson.rbx.ia.br
  rules:
    - backendRefs:
        - name: robsond
          port: 8080
