apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rbx-portal-tls
  namespace: rbx-portal
spec:
  secretName: rbx-portal-tls
  dnsNames:
    - portal.rbxsystems.ch
    - portal.rbx.ia.br
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
