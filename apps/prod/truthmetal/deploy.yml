apiVersion: apps/v1
kind: Deployment
metadata:
  name: truthmetal
  namespace: truthmetal
  labels:
    app.kubernetes.io/name: truthmetal
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: truthmetal
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: truthmetal
  template:
    metadata:
      labels:
        app.kubernetes.io/name: truthmetal
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: truthmetal
          image: ghcr.io/ldamasio/truthmetal
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
