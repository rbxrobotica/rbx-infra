# Strategos Site Deployment (product landing page for strategos.gr)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: strategos-site
  namespace: strategos
  labels:
    app.kubernetes.io/name: strategos-site
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: strategos
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: strategos-site
  template:
    metadata:
      labels:
        app.kubernetes.io/name: strategos-site
        app.kubernetes.io/component: frontend
    spec:
      imagePullSecrets:
        - name: ghcr-ldamasio
      containers:
        - name: strategos-site
          image: ghcr.io/ldamasio/strategos-site:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 101
            capabilities:
              drop:
                - ALL
