apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: eden-outbox-tls
  namespace: eden-outbox
spec:
  secretName: eden-outbox-tls
  dnsNames:
    - outbox.rbxsystems.ch
    - outbox.rbx.ia.br
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
