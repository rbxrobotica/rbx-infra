# WARNING: This is a TEMPLATE file.
# Replace all placeholders before applying to cluster.
#
# How to use:
#   1. Copy and fill in values
#   2. kubectl apply -f <filled-file> -n robson-v2
#   3. NEVER commit actual secrets to git
#
# For foundation mode, only database-url and projection-tenant-id are
# required. Binance keys are intentionally omitted to prevent any
# order execution.
apiVersion: v1
kind: Secret
metadata:
  name: robsond-secret
  namespace: robson-v2
  labels:
    app.kubernetes.io/name: robsond
    app.kubernetes.io/component: daemon
    app.kubernetes.io/part-of: robson
type: Opaque
stringData:
  database-url: "postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@paradedb:5432/robson_v2"
  projection-tenant-id: "YOUR_TENANT_UUID"
