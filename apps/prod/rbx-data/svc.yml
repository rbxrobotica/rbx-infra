apiVersion: v1
kind: Service
metadata:
  name: rbx-data
  namespace: rbx-data
  labels:
    app.kubernetes.io/name: rbx-data
    app.kubernetes.io/part-of: rbx-data
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rbx-data
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
