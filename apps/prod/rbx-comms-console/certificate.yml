# Certificate: comms.rbxsystems.ch + comms.rbx.ia.br
# rbx.ia.br DNS: ação manual necessária — zona não está no módulo OpenTofu
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rbx-comms-console-comms-tls
  namespace: rbx-comms
spec:
  dnsNames:
    - comms.rbxsystems.ch
    - comms.rbx.ia.br
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: rbx-comms-console-comms-tls
  usages:
    - digital signature
    - key encipherment
