# Secret Template: robson-secrets
# This is a template - actual secrets should be created via external-secrets or manually
apiVersion: v1
kind: Secret
metadata:
  name: robson-secrets
  namespace: robson
  labels:
    app.kubernetes.io/name: robson-secrets
    app.kubernetes.io/part-of: robson
  annotations:
    # Use this annotation if using external-secrets
    # external-secrets.io/provider: aws-secretsmanager
    # external-secrets.io/secret-name: robson/production
type: Opaque
stringData:
  # These are placeholder values - replace with actual secrets
  database-url: "postgresql://user:password@robson-paradedb:5432/robson"
  database-user: "robson"
  database-password: "CHANGE_ME"
  # Add other secrets as needed:
  # binance-api-key: ""
  # binance-api-secret: ""
  # django-secret-key: ""
