# ServiceAccount the CronJob pod runs as.
# The job only talks to S3 and the in-cluster LLM gateway over the network; it
# needs no Kubernetes API access, so no token is auto-mounted.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rbx-market-briefing
  namespace: rbx-market-briefing
automountServiceAccountToken: false
