# Certificate: console.rbxsystems.ch + console.rbx.ia.br
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rbx-console-tls
  namespace: rbx-console
spec:
  dnsNames:
    - console.rbxsystems.ch
    - console.rbx.ia.br   # rbx.ia.br DNS: ação manual necessária
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: rbx-console-tls
  usages:
    - digital signature
    - key encipherment
