apiVersion: v1
kind: Service
metadata:
  name: rbx-memory
  namespace: rbx-memory
  labels:
    app.kubernetes.io/name: rbx-memory
    app.kubernetes.io/part-of: rbx-memory
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rbx-memory
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
