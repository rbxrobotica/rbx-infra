apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rbx-memory-token
  namespace: rbx-memory
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: kubernetes-store
    kind: SecretStore
  target:
    name: rbx-memory-token
    creationPolicy: Owner
  data:
    - secretKey: token
      remoteRef:
        key: rbx-memory-token
        property: token
