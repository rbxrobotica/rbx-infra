# Pendências — 2026-03-25

---

## CRÍTICO — ArgoCD em Unknown (todos os apps)

### Sintoma
Todos os ArgoCD Applications estão com `sync.status: Unknown`. O problema surgiu após
o pod `argocd-redis` reiniciar (~5h antes da sessão de 2026-03-25), rotacionando a senha
e deixando o `argocd-application-controller` com erro `NOAUTH Authentication required`.

Reiniciar os componentes limpou o erro de NOAUTH nos logs, mas o status Unknown persiste.

### Impacto
- ArgoCD não faz auto-sync. Mudanças no `rbx-infra` não se propagam automaticamente.
- **Workaround atual:** aplicar manualmente com `kubectl apply -k apps/prod/<produto>/`

### Diagnóstico
```bash
kubectl --kubeconfig ~/.kube/config-rbx -n argocd get applications
kubectl --kubeconfig ~/.kube/config-rbx -n argocd logs argocd-application-controller-0 --tail=50
```

### Possíveis causas a investigar
1. O `argocd-application-controller` pode estar usando endereço de repo-server obsoleto (ClusterIP antigo).
2. A versão do ArgoCD pode estar com bug conhecido de recovery pós-Redis restart.
3. O Redis password pode estar divergindo entre pods.

### Fix sugerido
```bash
# Verificar versão do ArgoCD
kubectl --kubeconfig ~/.kube/config-rbx -n argocd get deploy argocd-server \
  -o jsonpath='{.spec.template.spec.containers[0].image}'

# Restart completo de todos os componentes
kubectl --kubeconfig ~/.kube/config-rbx -n argocd rollout restart \
  statefulset/argocd-application-controller \
  deployment/argocd-server \
  deployment/argocd-repo-server \
  deployment/argocd-applicationset-controller \
  deployment/argocd-notifications-controller

# Aguardar e checar
sleep 60
kubectl --kubeconfig ~/.kube/config-rbx -n argocd get applications
```

---

## IMPORTANTE — CI/CD do TruthMetal não existe

O TruthMetal (`ghcr.io/ldamasio/truthmetal`) é buildado e publicado manualmente:
```bash
cd ~/apps/truthmetal
podman build -t ghcr.io/ldamasio/truthmetal:latest .
podman push ghcr.io/ldamasio/truthmetal:latest
kubectl --kubeconfig ~/.kube/config-rbx -n truthmetal rollout restart deployment/truthmetal
```

**Criar GitHub Actions CI/CD** no repo `ldamasio/truthmetal` (ou `rbxrobotica/truthmetal`
após migração) seguindo o template em `docs/CONTAINER-REGISTRY.md`.

---

## IMPORTANTE — Migração dos repos de produto para `rbxrobotica`

Repos ainda em `ldamasio/`:
- `ldamasio/robson` → `rbxrobotica/robson`
- `ldamasio/strategos` → `rbxrobotica/strategos`
- `ldamasio/thalamus` → `rbxrobotica/thalamus`
- `ldamasio/truthmetal` → `rbxrobotica/truthmetal`
- `ldamasio/argos-radar` → `rbxrobotica/argos-radar`
- `ldamasio/eden` → `rbxrobotica/eden`

Para cada: transferir via GitHub Settings → Danger Zone → Transfer, depois atualizar:
- git remote local em `~/apps/<repo>`
- `repoURL` em `gitops/app-of-apps/<repo>.yml`
- `catalog/products.yml` campo `repo:`
- Imagem de `ghcr.io/ldamasio/` → `ghcr.io/rbxrobotica/` no CI e kustomization

---

## NORMAL — Deploy manual necessário após cada CI bem-sucedido

Enquanto o ArgoCD estiver Unknown, após cada CI que atualiza o kustomization, rodar:

```bash
cd ~/apps/rbx-infra && git pull
kubectl --kubeconfig ~/.kube/config-rbx apply -k apps/prod/rbx-ia-br/
```

---

## NORMAL — Eden sem repositório remoto

O CLI `eden` existe em `~/apps/eden` mas não tem remote no GitHub.
Criar repositório em `rbxrobotica/eden` e fazer push.

---

## Estado dos serviços em 2026-03-25

| Serviço | URL | Status |
|---------|-----|--------|
| rbx.ia.br | https://rbx.ia.br | Running ✅ |
| rbx.ia.br/produtos | https://rbx.ia.br/produtos | Running ✅ |
| rbx.ia.br/blog | https://rbx.ia.br/blog | Running ✅ |
| TruthMetal | api.truthmetal.rbx.ia.br | Running ✅ |
| Robson frontend | app.robson.rbx.ia.br | Running (verificar) |
| Robson backend | api.robson.rbx.ia.br | Running (verificar) |
| ArgoCD | — | Degradado (Unknown) |

## CI/CD por produto

| Produto | Registry | CI automático |
|---------|----------|---------------|
| rbx-ia-br | `ghcr.io/rbxrobotica/rbx-ia-br` | ✅ GitHub Actions |
| truthmetal | `ghcr.io/ldamasio/truthmetal` | ❌ Manual |
| robson | — | ❌ Verificar |
| strategos | — | ❌ Não iniciado |
| thalamus | — | ❌ Não iniciado |
| argos-radar | — | ❌ Não iniciado |
