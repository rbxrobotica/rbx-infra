# Pendências — 2026-03-24

Sessão encerrada em 2026-03-23. Tudo commitado e pushed em ambos os repos.
Esta lista cobre o que ainda precisa ser verificado ou executado na próxima sessão.

---

## Contexto da sessão anterior

- Toda a infra foi migrada do repo `robson` para `rbx-infra`
- ArgoCD `robson-prod` foi apontado para `rbx-infra/apps/prod/robson`
- `infra/` foi removido do repo `robson` (commit `3c291783`)
- Manifests quebrados do commit inicial foram corrigidos durante outage de produção

---

## 1. Verificar pattern-scan cronjob (CRÍTICO)

O comando do cronjob foi corrigido de `scan_patterns` para `detect_patterns`
no commit `3f393a3`, mas o ArgoCD pode não ter sincronizado ainda.

**Verificar:**
```bash
KUBECONFIG=~/.kube/config-rbx kubectl get jobs -n robson | grep pattern-scan
KUBECONFIG=~/.kube/config-rbx kubectl logs -n robson -l app.kubernetes.io/name=rbs-pattern-scan-cronjob --tail=20
```

**Esperado:** `Completed` sem erros. Se ainda `CrashLoopBackOff`, verificar o log
— pode ser que o comando correto seja outro (o Django sugere `detect_patterns`).

---

## 2. Verificar sites de produção (CRÍTICO)

Confirmar que todos os endpoints respondem corretamente após a migração:

| URL | Esperado |
|-----|----------|
| `https://app.robson.rbx.ia.br` | Frontend Robson carregando |
| `https://api.robson.rbx.ia.br/healthz` | `{"status": "ok"}` ou similar |
| `https://api.robson.rbx.ia.br/readyz` | `{"status": "ok"}` |
| `https://leandrodamasio.rbx.ia.br` | Site pessoal carregando |
| `https://rbx.ia.br` | Site RBX carregando |
| `https://rbxsystems.ch` | Site Swiss carregando |

---

## 3. Verificar paradedb e banco de dados (IMPORTANTE)

O PVC `rbs-paradedb-data` foi deletado pelo ArgoCD durante o outage.
O banco reiniciou vazio com um novo PVC (`data-robson-paradedb-0`).

**Verificar se o backend consegue migrar/operar:**
```bash
KUBECONFIG=~/.kube/config-rbx kubectl logs -n robson -l app.kubernetes.io/name=robson-backend --tail=50
```

Se houver erros de migração pendente, executar:
```bash
KUBECONFIG=~/.kube/config-rbx kubectl exec -n robson deploy/robson-backend -- python manage.py migrate
```

**Investigar se existe backup do banco antes do outage** (dados de trading podem ter sido perdidos).

---

## 4. Confirmar ArgoCD em Synced + Healthy

```bash
KUBECONFIG=~/.kube/config-rbx kubectl -n argocd get applications
```

**Esperado:** `robson-prod` com `Synced` e `Healthy`.
Se ainda `OutOfSync`, forçar sync:
```bash
KUBECONFIG=~/.kube/config-rbx kubectl -n argocd patch application robson-prod \
  --type merge -p '{"operation":{"initiatedBy":{"username":"admin"},"sync":{"revision":"HEAD"}}}'
```

---

## 5. (Opcional) Aplicar root App of Apps

O ArgoCD ainda não gerencia a si mesmo via GitOps. O arquivo existe em:
`gitops/app-of-apps/root.yml`

Antes de aplicar, corrigir `gitops/app-of-apps/applications.yml`:
- O destination namespace é `argocd`, que não está na lista de destinos do AppProject `rbx-applications`
- Solução: mover `applications.yml` para o projeto `rbx-platform` ou adicionar `argocd` como destino permitido

Só fazer isso após confirmar que todos os itens 1–4 estão verdes.

---

## 6. Tunnel SSH no Windows 11

Para conectar ao cluster a partir do Windows 11:

```powershell
ssh -o ExitOnForwardFailure=yes -N -L 6443:localhost:6443 root@rbx.ia.br
# Em outro terminal:
$env:KUBECONFIG = "$HOME\.kube\config-rbx"
kubectl get nodes
```

O arquivo `~/.kube/config-rbx` precisa estar copiado do Linux Mint
ou recriado apontando para `server: https://127.0.0.1:6443`.

---

## Estado dos pods ao encerrar a sessão (2026-03-23)

| Recurso | Status |
|---------|--------|
| `robson-backend` | Running |
| `robson-frontend` | Running |
| `robson-paradedb-0` | Running (banco vazio — ver item 3) |
| `robson-redis` | Running |
| `lda-prod` | Running |
| `rbx-ia-br-prod` | 2/2 Running |
| `rbs-stop-monitor-cronjob` | Completed ✅ |
| `rbs-trailing-stop-cronjob` | Completed ✅ |
| `rbs-pattern-scan-cronjob` | Pendente sync (ver item 1) |
