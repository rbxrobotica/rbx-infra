# Pendências — 2026-03-29

Sessão retomada após limite de uso. Continuação do trabalho de 2026-03-28.

---

## ✅ Concluído nesta sessão

### ArgoCD OutOfSync (herança 2026-03-28)
- Todos os Applications em Synced ✅
- `argos-radar` e `thalamus` removidos (apps sem imagem publicada, ainda em planejamento)
  - Serão recriados no futuro apontando para `ghcr.io/rbxrobotica/` quando prontos

### robson-prod — diagnóstico e correção de infraestrutura
- **Causa raiz do `Connection refused`**: kustomize `commonLabels` adicionava `spec.selector`
  ao serviço externo `robson-paradedb`, fazendo o kubernetes sobrescrever os Endpoints
  manuais (que apontam para jaguar) com os pods do namespace
- **Fix**: patch no `kustomization.yml` remove o `spec.selector` do serviço externo após
  o commonLabels ser aplicado
- **StatefulSet `robson-paradedb` órfão removido** do cluster (era sobra da arquitetura
  anterior onde paradedb rodava dentro do k8s; a decisão é mantê-lo no jaguar)
- **PVC `data-robson-paradedb-0` removido** (10Gi, local-path, tiger — sem dados relevantes,
  banco estava vazio/sem o database correto)

---

## ✅ robson-prod — resolvido

### Jaguar: ParadeDB configurado
- User `robson` e database `robson` já existiam no jaguar
- Senha foi sincronizada entre `rbs-django-secret` e o postgres do jaguar
- GRANT ALL ON SCHEMA public aplicado para o user `robson`
- Migrations rodaram automaticamente no startup do Django

### Correções de infra aplicadas
- `commonLabels` substituído por `labels/includeSelectors:false` em `kustomization.yml`
  para evitar injeção de `spec.selector` no serviço externo paradedb
- Deployments recriados para alinhar selectors imutáveis com novo kustomize output
- EndpointSlice corretamente apontando para jaguar (`161.97.147.76:5432`)
- Node bengal cordonado (COMPROMETIDO — não agendará novos pods)

### pattern-scan cronjob com ImportError
- `detect_patterns.py` tenta importar `BinanceClient` de `api.models` mas a classe não existe
- **É um bug no código do robson** (não é infra), precisa ser corrigido no repo `ldamasio/robson`
  (ou `rbxrobotica/robson` após migração)
- Após correção, novo build e push da imagem

---

## 📋 Backlog (não urgente)

### Robson — objetivo mínimo
Manter no ar para: cadastro de novos usuários, acesso à DEMO, contratação do serviço.
Não focar em funcionalidades de trading em si por ora.

### Migração de repos para rbxrobotica
Ver `docs/PENDING-2026-03-25.md` — ainda pendente para robson, truthmetal, strategos.

### ktunnel removido
Acesso ao cluster agora via IP dinâmico da Claro (allowlist no firewall do cluster).
Se kubectl não responder, informar IP atual para liberação.

---

## Estado dos Applications ArgoCD ao encerrar (2026-03-29)

| Application | Sync | Health |
|-------------|------|--------|
| argocd | Synced | Healthy ✅ |
| cert-manager | Synced | Healthy ✅ |
| lda-prod | Synced | Healthy ✅ |
| platform | Synced | Healthy ✅ |
| rbx-ia-br | Synced | Healthy ✅ |
| rbxsystems-ch | Synced | Healthy ✅ |
| robson-prod | Synced | Degraded ⚠️ (aguardando DB jaguar) |
| root | Synced | Healthy ✅ |
| truthmetal | Synced | Healthy ✅ |
| argos-radar | Removido ✅ |
| thalamus | Removido ✅ |
